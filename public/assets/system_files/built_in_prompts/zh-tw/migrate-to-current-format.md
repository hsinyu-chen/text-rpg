請把本世界書檔案遷移成最新格式。逐檔處理，**所有變更必須以工具呼叫完成**（getFileOutline / readSection / replaceSection / insertSection / searchReplace），不可只回覆說明。

## 1. `4.資產.md`

最終結構應與下列範本一致：

````markdown
# 資產

> **適用範圍**：本檔僅記錄**主角擁有**的金錢、房產與據點佈局。據點各房間下可列出**寄存於該地點的長期物資**（與 `9.物品欄.md` 區別：物品欄為主角隨身物，本檔為非隨身儲存）。**NPC 與同伴的私有資產**請寫至 `3.人物狀態.md` 該角色條目下，不寫於此。

## 存檔格式

```
# 動產

* **目前持有現金**: **[金額][貨幣單位]** (截至 Act.[編號])

# 不動產

## [據點名稱]

* **位置**: [位置]
* **描述**: [詳細描述]
* **配置**:
    * **[樓層]**:
        - **[房間名稱]**: [功能與內容；可列存放於此的長期物資]
```

---

# 動產

（實際資料；若無則填「無」）

---

# 不動產

（實際資料；若無則填「無」）
````

要做的修正：
- 若頂層 H1 是「# 可動資產」，重新命名為「# 動產」。
- 拆掉 `## 初始資產`、`## 初始不動產` 等中介標題，把內容直接搬到對應 H1 之下。
- **不要更動實際資料**（金額／據點佈局），只調整結構標題。
- 若舊檔尚無 `## 存檔格式` 或 `> 適用範圍` 區塊，依上述範本補上；若 `## 存檔格式` 原本分散在 `# 動產` 與 `# 不動產` 之下，請合併為單一區塊置於頂層 `# 資產` 之下。

## 2. `3.人物狀態.md`

為每個**非玩家角色**（即 `**玩家角色**: 是` 的條目除外）在 `### 核心價值觀與行為準則` **之前**插入下列子節區（若該 NPC 已有此節區則跳過）：

```
### 已知持有重要物品

- **更新時間**：（待補；目前未觀察到具體物品）
- （目前無已記錄之重要物品）
```

**主角條目絕對不要**新增此節區。

## 3. `9.物品欄.md`

新規則：本檔僅記錄**主角擁有且隨身**之物。請審視內容：
- 若有 NPC 私人物品條目（例如「莉塔的項鍊」），將該物品移至 `3.人物狀態.md` 該 NPC 的 `### 已知持有重要物品` 中，並從本檔移除。
- 若有「家裡寶箱」「旅館存物櫃」等寄存於據點的容器，從本檔移除，內容寫入 `4.資產.md` 對應據點佈局之下（若該據點不存在，先在 `4.資產.md` 新增）。
- 不確定的條目保留並於最終報告中提及，請使用者自行判斷。

## 結束

完成所有遷移後，呼叫 `submitResponse` 並逐檔回報：
- 各檔案做了哪些變更（或未動）。
- 任何不確定、需使用者自行判斷的條目。
