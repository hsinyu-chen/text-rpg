
- **0.Stats.yaml** (a 10th file, beyond the 9 above — you must author it too) — A starting **numeric stats ledger** is included with this world. It is the structured number system the engine tracks per turn (HP, MP, NPC affinity, etc.). **Tailor it to this world** instead of leaving the generic template:
  - Define genre-appropriate stats. Each stat entry uses `type` (`scalar` = one number; `map` = one number per subkey), `value` (the **baseline**), `min`, `max`, `desc`, and `color` (any CSS color; quote a `#hex`). Pick stats that fit the genre/tone (e.g. `sanity` for horror, `notoriety` for a crime world, `fuel`/`hull` for sci-fi) — drop or rename ones that don't fit.
  - For `map`-type stats such as `affinity`, add one subkey per **known NPC you created in `3.Character_Status.md`**, and set `allow_new_item: true` where the engine should be able to add newly-met NPCs.
  - Provide a short `rules:` block describing how the stats rise and fall in this world, and add **1–2 `events`** with a `condition`, a `type` (`level` fires whenever truthy; `edge` fires once on a false→true crossing), and a `trigger` narration line.
  - The ledger stores **baseline values only** — the live current value is derived from per-turn deltas during play, so do not pre-bake "current" numbers here; set sensible starting baselines.
  - **CRITICAL — this file is the explicit exception to the "do NOT use `replaceFile`" rule above.** Section tools (`replaceSection` / `insertSection`) are **rejected** on this YAML file because it has no Markdown headings. You **MUST** write `0.Stats.yaml` **whole-file with `replaceFile`**. If a write is rejected for a YAML syntax error, read the error and rewrite the entire file with `replaceFile`.
