> ⚠️ **This turn is a correction-driven re-run** ⚠️
>
> Previous turn's `<System>` correction:
>
> ```
> {{CORRECTION_TEXT}}
> ```
>
> ## Re-run rules (hard)
> 1. Re-generate the scene for the **same player action**. Discard the previous version; narrate from the corrected world state.
> 2. The correction above is top priority; conflicting prior content yields to it.
> 3. Narrator prose, `*_log`, and `scene_header` must match the correction.
> 4. If the correction touches gear/inventory/status, write a corresponding `校正` entry in `inventory_log` or `character_log`.
> 5. Do not apologize or mention "correction" in the prose — this is the correct version.
