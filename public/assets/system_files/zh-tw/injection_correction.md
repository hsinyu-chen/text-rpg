> ⚠️ **本回合為劇情修正重跑** ⚠️
>
> 上一回合 `<系統>` 介入並要求修正，內容如下：
>
> ```
> {{CORRECTION_TEXT}}
> ```
>
> ## 重跑規則（硬性）
> 1. 你正在以**同樣的玩家行動**重新生成劇情。請完全忽略上一輪舊版劇情，從修正後的世界狀態出發重新敘述。
> 2. 上述修正規則為**最高優先**。原 `character_log`、`inventory_log`、設定描述若與其衝突，**一律以修正規則為準**。
> 3. Narrator 寫出的 prose、`*_log` 條目、`scene_header`（若適用）必須與修正後設定一致。
> 4. 若修正涉及主角裝備／物品／狀態，本回合的 `inventory_log` 或 `character_log` **必須**寫入對應的 `校正` 條目，使硬性事實與修正紀要並存。
> 5. 不要在劇情中道歉、說明或提及「修正」這件事 —— 對玩家而言，這就是正確的劇情版本。
