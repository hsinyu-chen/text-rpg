> ⚠️ **本回合為劇情修正重跑** ⚠️
>
> 上一回合 `<系統>` 介入並要求修正，內容如下：
>
> ```
> {{CORRECTION_TEXT}}
> ```
>
> ## 重跑規則（硬性）
> 1. 以**同樣的玩家行動**重跑。完全忽略上一輪舊劇情，從修正後的世界狀態重新敘述。
> 2. 上述修正規則最高優先；與原設定衝突時一律以修正為準。
> 3. Narrator 的 prose、`*_log`、`scene_header` 必須與修正一致。
> 4. 涉及主角裝備／物品／狀態的修正，本回合 `inventory_log` 或 `character_log` 必須寫對應的 `校正` 條目。
> 5. 不要在劇情中道歉或提及「修正」—— 對玩家而言這就是正確版本。
