/**
 * Reserved folder names that should be hidden from UI and prevented from being created
 * to avoid conflict with internal system folders in Cloud Storage.
 */
export const RESERVED_FOLDER_NAMES = ['saves', 'books_v1'];
